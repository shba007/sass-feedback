{
  "name": "sass-feedback-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -c \"bgBlue.bold,bgGreen.bold\" \"npm:watch-*\"",
    "watch-ts": "tsc -w",
    "watch-node": "node --watch dist/main.js",
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.16",
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "mongoose": "^8.1.1"
  }
}
